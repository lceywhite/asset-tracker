# 1.0.0-beta.1：单人 MVP iPhone 测试版

状态：代码完成，等待 Mac/Xcode 真机签名验收  
日期：2026-07-15

## 交付内容

- 保留并统一“物品 / 行程 / 社区 / 我的”四个核心 Tab，在底部中央保留可新增物品或行程的全局“+”操作。
- 重做“我的”：个人资料、统计、物品视图偏好、完整备份恢复、数据清空、隐私边界、安装状态和版本日志。
- DB v5 新增 `checkSessions`；旧行程条目补 ID、剥离历史勾选状态。
- 出发和返程核对独立记录，可恢复进行中的核对；删除行程时同步删除核对记录。
- 完整备份升级到 v2并兼容 v1；iOS 端通过原生分享面板导出。
- 接入 Capacitor 8，生成 Xcode 工程，使用 SPM 和 6 个原生插件。
- iOS 15+、竖屏、安全区、相机用途说明、离线打包资源。
- 增加产品经理可读的应用内版本记录和文档日志。

## 自动验证

- `npm run check`：ESLint、Prettier、5 项测试和 Vite 生产构建通过。
- `npm run ios:sync`：网页资源和 6 个 Capacitor 插件成功同步到 iOS 工程。
- `npm run smoke`：390×844 下完成 DB v4→v5、添加物品、创建行程、独立核对、四 Tab、备份 v2和横向溢出检查。

## 尚待人工验证

当前开发环境为 Windows，不能运行 Xcode。需要在 macOS + Xcode 26 上完成 SPM 解析、签名、真机安装、相机、分享、触觉、冷启动和飞行模式验收。
