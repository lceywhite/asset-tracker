# Asset Tracker 项目专属规范

更新日期：2026-08-06
适用范围：本仓库 `asset-tracker` 的产品边界、技术约束、版本基线、验收与项目文档。

通用的设计确认、数据迁移、验证、版本、Git 与沟通规则继承自全局 `AGENTS.md` 的“通用工程开发协作规范”。本文件只记录本项目的特例。

## 1. 产品边界

- 产品定位是本地优先的单人资产与行动记录工具：记录物品、查找位置、准备或核对携带物品，并可靠备份与恢复。
- 保持“物品、行程、社区、我的”四个核心 Tab 和底部中央加号；社区在单人阶段只保留诚实入口，不虚构社区内容或云端能力。
- 不擅自引入云同步、账号、多人共享、社区内容、系统通知、地图追踪或外部数据上传；需要这些能力时先明确产品范围、权限和用户授权。

## 2. 设计与模块基线

- 复杂界面延续物品模块 v3 的克制移动端语言：信息层级清晰、核心动作少、填写路径短；先保证单人离线使用，再考虑高级功能。
- 当前有效设计与工程状态以 `docs/00-planning/current-development-status.md` 为准。
- 版本名称以 `docs/00-planning/version-management.md` 为准：物品模块 v3、行程模块 v3 均已接入；行程 v2 原型第 11 版仅作为历史设计来源。
- “行程模块 v2 原型第 11 版”不能缩写为“行程 v11”；`Plan.modelVersion 3` 也不能代替产品模块版本。

## 3. 数据与验收特例

- 当前产品使用 IndexedDB。本项目的数据结构变化需要明确实体 `modelVersion`、IndexedDB 升级和备份兼容；具体当前版本见版本管理文档。
- Windows 端必须可网页预览和调试；iOS 以 Mac/Xcode 直接安装到 iPhone 为发布验收目标。
- iOS 真机测试统一推迟到单人 MVP 功能完整且 Web 回归通过后集中执行；开发期间只保持 Capacitor 工程可同步，不把未执行的 Xcode/iPhone 验收标记为通过。
- Windows 开发环境使用 `npm.cmd` 执行脚本，避免 PowerShell 的脚本执行策略问题。
- iPhone、相机、离线冷启动、拖动布局等项目验收项，只有实际执行后才能标记为通过。

## 4. 项目文档

- 文档按开发阶段放在 `docs/` 对应编号目录；当前入口是 `docs/README.md`。
- 非技术迭代日志必须说明“哪个版本改变了什么、影响哪些功能和界面、用户感知是什么”。
- 设计确认、工程接入完成或跨模块切换时，归档本项目用户与助手可见的对话原文；系统指令、环境注入、内部推理和工具输出不归档。
- 当前对话与版本归档入口见 `docs/00-planning/conversation-archive-index.md`。
